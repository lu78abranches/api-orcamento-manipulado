# Nome base do projeto como fallback global
spring.application.name=api-orcamento-manipulado

# Perfil padrão ativo se nenhum for especificado por variável de ambiente (muda para prod no Render)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# Porta de execução da API
server.port=8082

# Gerenciamento de Segredos e Arquivo .env
spring.config.import=optional:file:.env[.properties]

# Configuração da API do Google Gemini (Compartilhada entre os perfis)
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
gemini.api.key=${GEMINI_API_KEY}

# Configuração comum do JWT
api.security.token.secret=${JWT_SECRET:minha-chave-secreta-padrao-super-longa-para-o-jwt-da-farmacia-de-manipulacao}

